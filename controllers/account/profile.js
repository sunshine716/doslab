const get = (req, res) => {
    res.send('GET: TODO - Implement /account/profile GET handler');
};

const post = (req, res) => {
  res.send('POST: TODO - Implement /account/profile POST handler');
};

module.exports = {
  get,
  post
};